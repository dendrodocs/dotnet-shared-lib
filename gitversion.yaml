next-version: 0.1.0
branches:
  release:
    regex: releases?[/-]
    label: rc
  pull-request:
    mode: ContinuousDeployment
    regex: ^(pull-requests|pull|pr)[\/-](?<Number>\d*)
    label: pr{Number}
    prevent-increment:
      of-merged-branch: false
